name: convertpdf
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  printing: 5.11.1  # <- NO ^ HERE. PINNED VERSION
  pdf: 3.10.7       # <- NO ^ HERE. PINNED VERSION

flutter:
  uses-material-design: true
